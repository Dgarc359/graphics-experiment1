extern crate sdl2;

pub struct Asset {
    pub width: usize,
    pub height: usize,
    pub buffer: Vec<u8>,
}


fn get_image() -> bool {
    true
}


pub struct Assets {
    pub list: Vec<Assets>,
}
